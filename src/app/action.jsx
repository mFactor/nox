/**
 * Set Nox application context
 * @param {object} server - Context includes endpoint, name
 */
/*
const setContext = (name, endpoint) => ({
  type: 'SET_CONTEXT',
  name,
  endpoint,
});

export { setContext };
*/
