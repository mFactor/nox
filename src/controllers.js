/**
 * Global controller handler
 * Import/Export any new controllers here, to make available in server.js
 */
import base from './base/api/controller';
import opcua from './opcua/api/controller';

export { base, opcua };
